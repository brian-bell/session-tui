/target

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Beads local activity log (machine-local churn; issue sync uses refs/dolt/data)
.beads/interactions.jsonl

# Beads passive JSONL export (issue sync uses refs/dolt/data)
.beads/issues.jsonl
